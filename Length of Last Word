class Solution(object):
    def lengthOfLastWord(self, s):
        words = s.split()
        if not words:
            return 0

        # Return the length of the last word
        return len(words[-1])
